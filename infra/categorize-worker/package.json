{
  "name": "categorize-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node build.mjs",
    "deploy": "npm run build && sam deploy",
    "deploy:guided": "npm run build && sam deploy --guided"
  },
  "dependencies": {
    "@prisma/client": "^6.17.1",
    "@aws-sdk/client-secrets-manager": "^3.740.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "@types/aws-lambda": "^8.10.145",
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0",
    "prisma": "^6.17.1"
  }
}
